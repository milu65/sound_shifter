class GeneralAudio:
    def __init__(self, sample_rate, num_channels, data):
        self.sample_rate = sample_rate
        self.num_channels = num_channels
        self.data = data
# TODO: add fields: byte_rate, bits_per_sample, wav_block_align, bit_depth
